name: Keep Render Backend Awake

on:
  schedule:
    - cron: '7,17,27,37,47,57 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend
        run: |
          curl --fail --show-error --location \
            https://portfolio-tzvg.onrender.com/api/about
